package services

import (
	"skybox-backend/internal/models"
)

// AuthencateUser checks email and password
func AuthenticateUser(email, password string) (*models.User, error) {
	// ...

	return nil, nil
}
