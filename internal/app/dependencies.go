package app

import (
	"skybox-backend/internal/repositories"
)

// TODO: This file will contain all the dependencies that will be injected into the application
// For example: Controllers (handlers), database, cache, etc.

// initDependencies will initialize all the dependencies
func initDependencies(db *repositories.MongoClient) {
	// ...
}
